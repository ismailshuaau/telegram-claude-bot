# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your-bot-token-here
ALLOWED_USER_IDS=123456789,987654321

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# AUTHENTICATION METHOD - Choose one of two options:
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# AUTH_METHOD Options:
# - 'auto' (default): Auto-detect (prefers CLI if available, falls back to API)
# - 'cli': Use Claude Code CLI (requires Claude subscription: Pro/Max/Team/Enterprise)
# - 'api': Use Anthropic API (requires API key, pay-per-use billing)
AUTH_METHOD=auto

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# OPTION 1: Claude Subscription (CLI Method)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# Requirements:
# - Claude Pro ($20/mo), Max ($60/mo), Team, or Enterprise subscription
# - Claude Code CLI installed: npm install -g @anthropic-ai/claude-code
# - Logged in: claude-code login
#
# Setup:
# 1. Set AUTH_METHOD=cli (or leave as 'auto')
# 2. No API key needed!
# 3. Bot will use your Claude subscription
#
# Benefits:
# - Fixed monthly cost (included in subscription)
# - No per-request billing
# - Same Claude account you use personally
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# OPTION 2: Anthropic API (API Key Method)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# Requirements:
# - Anthropic Console account: https://console.anthropic.com/
# - API key from console
#
# Setup:
# 1. Get API key from https://console.anthropic.com/
# 2. Set ANTHROPIC_API_KEY below
# 3. Set AUTH_METHOD=api (or leave as 'auto')
#
# Benefits:
# - Pay-per-use (~$0.06 per coding session)
# - No subscription needed
# - Good for light usage
# - Better for production/scaling
#
# ANTHROPIC_API_KEY=sk-ant-api03-xxxxx
#
# Cost Example:
# - Light usage (10 sessions/mo): ~$0.60/month
# - Medium usage (50 sessions/mo): ~$3/month
# - Heavy usage (200 sessions/mo): ~$12/month
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# If using API method, set your API key here:
# ANTHROPIC_API_KEY=your-anthropic-key-here

# Project Paths (adjust if your project is elsewhere)
PROJECT_ROOT=/home/ubuntu/project
BACKEND_PATH=/home/ubuntu/project/transcription-platform
FRONTEND_PATH=/home/ubuntu/project/expo-voice-analytics-mobile-app

# Bot Settings
CLAUDE_MODEL=claude-sonnet-4-5-20250929
CLAUDE_TIMEOUT=300
MAX_REQUESTS_PER_MINUTE=10
SESSION_TIMEOUT=3600
LOG_LEVEL=INFO

# Features
ENABLE_NOTIFICATIONS=true
NOTIFY_ON_ERROR=true
NOTIFY_ON_COMPLETION=true

# Git Settings (optional automation)
GIT_AUTO_COMMIT=false
GIT_AUTO_PUSH=false
